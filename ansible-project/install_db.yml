---
- name: Развертывание БД PostgreSQL
  hosts: webservers
  become: yes
  vars_files:
    - secrets.yml
  tasks:
    - name: Показывать секретный пароль из сейфа
      ansible.builtin.debug:
        msg: "Наш расшифрованный пароль : {{db_password }}"
  roles:
    - install_postgres
